#include <iostream>
using namespace std;
int main() {
	int a, n, m, x;
	cin >> n >> m >> a;
	if (a <= 0) cout << "Error";
	else {
		int res1 = n / a;
		int res2 = m / a;
		if (n % a > 0) res1++;
		if (m % a > 0) res2++;
		x = res1 * res2;
		std::cout << x;
	}
	return 0;
}
